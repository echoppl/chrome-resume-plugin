# 插件使用文档

## 目录

1. [安装与配置](#安装与配置)
2. [Boss直聘简历收集](#boss直聘简历收集)
3. [猎聘简历收集](#猎聘简历收集)
4. [常见问题](#常见问题)

---

## 安装与配置

### 安装步骤

1. **构建扩展包**：`npm run build`，生成 `dist` 文件夹
2. **打开扩展管理页面**：Chrome 地址栏输入 `chrome://extensions/`
3. **开启开发者模式**：右上角点击「开发者模式」开关
4. **加载扩展**：点击「加载已解压的扩展程序」→ 选择项目的 `dist` 文件夹

### 首次使用前准备

1. **登录 HRM 系统**：确保已在浏览器中登录 HRM 系统（`hrm-sit6.fcbox.com`），插件通过 cookies 获取认证
2. **登录招聘平台**：确保已登录 Boss直聘或猎聘网

---

## Boss直聘简历收集

### 操作流程

#### 第一步：打开简历预览页面

1. 登录 Boss直聘（`https://www.zhipin.com`）
2. 进入招聘管理页面，打开候选人简历预览

#### 第二步：点击插件图标

1. 点击浏览器工具栏中的插件图标
2. 插件自动检测当前页面的简历 PDF（顶部会显示状态条）

#### 第三步：选择需求岗位并提交

1. 在「**关联需求岗位**」下拉框中选择对应的招聘需求（必选）
2. 选择后下方会显示该需求的部门、岗位等信息标签
3. 点击「**🚀 提交简历**」按钮上传

### 界面说明

| 区域 | 说明 |
|------|------|
| 顶部状态条 | 蓝色=检测中 / 绿色=已检测到PDF / 红色=未检测到 |
| PDF预览 | 「📄 点击查看简历PDF」可点击文字，在新标签页打开 PDF |
| 需求选择 | 必选项，选择后显示部门/岗位标签 |
| 橙色提示 | 未选择需求岗位时显示 |
| 提交按钮 | 渐变紫色按钮，选中需求后可用 |

### 注意事项

- **无需填写手机号**：已优化流程，直接选择需求岗位即可提交
- **PDF 自动检测**：打开简历页面后插件自动检测，无需手动触发扫描
- **重复提交保护**：同一简历 5 秒内不会重复上传
- **未检测到 PDF**：刷新简历页面后重新点击插件图标尝试

---

## 猎聘简历收集

### 操作流程

#### 第一步：打开简历查看页面

1. 登录猎聘网（`https://www.liepin.com`）
2. 进入简历管理页面，打开候选人简历详情

#### 第二步：点击插件图标

1. 点击浏览器工具栏中的插件图标
2. 插件自动拦截简历 API 并提取候选人信息

#### 第三步：确认信息并提交

1. 候选人信息以卡片形式自动展示：
   - 姓名、头像
   - 性别、年龄、工作年限、学历等标签
   - 手机号、邮箱（如有）
2. 在「**关联需求岗位**」下拉框中选择对应的招聘需求（必选）
3. 点击「**🚀 提交简历**」按钮上传

### 界面说明

| 区域 | 说明 |
|------|------|
| 简历状态条 | 显示简历附件是否检测到（绿色=已就绪 / 红色=未检测） |
| 候选人卡片 | 头像、姓名、详细信息标签（性别/年龄/工作年限/学历） |
| 联系方式 | 手机号、邮箱（如有） |
| 需求选择 | 必选项，选择后显示部门/岗位标签 |
| 提交按钮 | 渐变紫色按钮，选中需求且简历就绪后可用 |

### 猎聘简历字段（自动填充）

| 字段 | 说明 |
|------|------|
| 姓名 | 候选人姓名 |
| 性别 | 男/女 |
| 年龄 | 岁数 |
| 学历 | 最高学历（初中及以下到博士） |
| 工作经验 | 工作年限 |
| 手机号 | 候选人联系电话 |
| 邮箱 | 候选人邮箱地址 |
| 所在城市 | 候选人当前所在城市 |

### 注意事项

- **自动拦截 API**：插件会拦截猎聘简历查看 API 请求，无需手动操作
- **简历附件**：需检测到简历附件才可提交（状态条显示绿色）
- **需求列表**：从 HRM 系统获取，需确保已登录

---

## 常见问题

### Q1：插件无法识别简历页面

**原因**：插件只支持 Boss直聘和猎聘两个平台

**解决方案**：
- 确认当前页面是 Boss直聘（`zhipin.com`）或猎聘（`liepin.com`）
- 刷新页面后重新点击插件图标

### Q2：未检测到 PDF 链接

**原因**：简历页面还未完全加载，或 PDF 链接在深层 DOM 中

**解决方案**：
1. 等待页面完全加载后再点击插件图标
2. 刷新简历页面后重试

### Q3：上传失败

**原因**：网络问题、HRM 系统未登录、或未选择需求岗位

**解决方案**：
1. 确认已登录 HRM 系统（`hrm-sit6.fcbox.com`）
2. 检查表单是否显示橙色提示「请先选择关联需求岗位」
3. 查看弹窗中的红色错误状态提示

### Q4：猎聘简历信息未自动填充

**原因**：API 拦截失败

**解决方案**：
1. 刷新简历页面，确保插件有足够时间拦截请求
2. 检查控制台是否有报错

### Q5：提交按钮是灰色不可点击

**原因**：未选择「关联需求岗位」或简历未检测到

**解决方案**：
1. 确认顶部简历状态条为绿色
2. 从下拉框中选择一个需求岗位

---

## 更新日志

### 2026-07-03 界面优化

- **Boss直聘**：去掉手机号输入框，改为选择需求岗位后直接提交；PDF 链接改为可点击文字展示
- **猎聘**：优化候选人信息展示为卡片样式；统一提交流程，只需选择需求岗位
- **UI 统一**：简历状态实时显示；提交按钮改为渐变紫色风格

---

技术支持：如遇问题请联系开发团队。
